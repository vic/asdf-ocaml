#!/usr/bin/env bash

cmd="curl -s"
if [ -n "$GITHUB_API_TOKEN" ]; then
  cmd="$cmd -H 'Authorization: token $GITHUB_API_TOKEN'"
fi

echo $($cmd https://api.github.com/repos/ocaml/ocaml/releases | sed -n 's/.*"tag_name": "\(.*\)",/\1/p' | sort -t. -n -k2,3)
