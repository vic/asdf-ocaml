#!/usr/bin/env bash

eval $(env SHELL=$0 OPAMROOT=$ASDF_INSTALL_PATH $ASDF_INSTALL_PATH/$ASDF_INSTALL_VERSION/bin/opam config env --root=$ASDF_INSTALL_PATH; echo "OPAMROOT=$ASDF_INSTALL_PATH; export OPAMROOT")
